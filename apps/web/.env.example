# Base URLs for the two backend services this app proxies to (see CONTRACT.md).
CORE_SERVICE_URL=http://localhost:8000
AI_SERVICE_URL=http://localhost:8100

# Must match services/core's JWT_SECRET exactly — this app verifies (but
# never issues) the session JWT that Core signs at login.
JWT_SECRET=dev-only-change-me
